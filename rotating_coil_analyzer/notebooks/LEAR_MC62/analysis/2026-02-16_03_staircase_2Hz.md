# MC62 -- LEAR C-Shaped Dipole -- 03_staircase_2Hz -- Rotating Coil Analysis

## Overview
Full hysteresis staircase measurement of MC62 (no shims), streaming at 2 Hz rotation (512 samples/turn).
Current cycle: precycles (0->-200->+200->-200->0 A) then staircase 0->+200->0->-200->0 A in 20 A steps.
Ramp rate: 1 A/s. 41 staircase plateaus, ~740 turns each (~370 s per step).

## Configuration

| Parameter | Value |
|-----------|-------|
| Magnet | MC62 (red bulk C-shaped dipole, no shims) |
| Rotation | 2 Hz (120 rpm), 512 samples/turn, 0.5 s/turn |
| R_ref | 0.033 m |
| N_LAST_TURNS | 340 |
| Pipeline | `dri`, `rot`, `cel`, `fed` (legacy drift) |
| Parity pipeline | `dri`, `rot`, `cel`, `fed`, `dit` (signed, matching FFMM C++ native) |
| Plateau threshold | 0.5 A (block-averaged), min_length=50, merge gap<100 |

## Data Summary
- 35828 total turns (587 MB per binary file)
- 3 precycle groups (at +/-200 A) + 41 staircase plateaus
- Precycle: turns 0--3785, Staircase: turns 3786--35827
- All 41 plateaus "good" quality (Integral PCB)
- **Central PCB has severe numerical issues** (corrupt values at several current levels)

## Key Results (Integral PCB)

| I (A) | B1 (T) | b2 (units) | b3 (units) | TF (T/kA) |
|-------|--------|-----------|-----------|-----------|
| +100 (asc) | 0.1242 | -13.20 | -11.97 | 1.2424 |
| +200 (asc) | 0.2185 | -13.44 | -11.81 | 1.0926 |
| +100 (desc) | 0.1250 | -12.57 | -11.72 | 1.2502 |
| -200 (desc) | -0.2185 | -13.55 | -11.85 | 1.0925 |

## Golden Standard Parity Check (vs FFMM C++)

Parity uses `dit` (di/dt correction) with `signed=True` to match the FFMM C++ native
threshold logic (`crr > 0.1 && cm > 10`).

### B_main
- **Median |diff|**: 2.8e-17 T (machine precision)
- **Mean |diff|**: 6.2e-17 T (machine precision)
- **Max |diff|**: 1.8e-13 T (machine precision)
- **Mean |rel diff|** (|B|>0.01 T): 1.3e-15

### Harmonics (b2, b3 at R_ref=0.33 m)
- **b2**: RMS diff = 0.0000 units, max |diff| = 0.0000
- **b3**: RMS diff = 0.0000 units, max |diff| = 0.0004
- **All 41 plateau middle turns**: exact agreement (dB1=0.0, db2=0.00, db3=0.00)

### Note on FFMM Central
FFMM Central results are all NaN because the measurement-embedded Central Kn file is all-zeros (bug). Parity check is Integral only.

## Observations
1. **b2 ~ -13 units** at high current, consistent with test 02 (without shims, -15 units). The difference vs test 02 may be due to different settling dynamics at 2 Hz.
2. **b3 ~ -12 units**, slightly improved vs test 02 (-17 units). This difference warrants investigation.
3. **Hysteresis**: B1 shows clear ascending/descending branch splitting, especially at high current (saturation regime).
4. **TF at 200 A** = 1.093 T/kA, matching test 02 (1.132 T/kA) order of magnitude (small difference from averaging window).
5. **Central PCB data is unreliable** for this measurement -- shows wildly corrupt values at certain current levels.
6. **Parity with FFMM is perfect** -- machine-precision agreement on ALL 35826 turns (B_main max |diff| = 1.8e-13 T) after enabling `dit` with signed thresholds.

## Comparison with Previous Tests

| Test | b2 (units) | b3 (units) | TF@200A (T/kA) |
|------|-----------|-----------|-----------------|
| 01 (with shims, 1 Hz) | -151 | -17 | 1.132 |
| 02 (no shims, 1 Hz) | -15 | -17 | 1.132 |
| 03 (no shims, 2 Hz) | -13 | -12 | 1.093 |

### Inductance Analysis (Section 10b)
Compares apparent and differential inductance from the B-H curve:
- **L_app = B1/I** (Transfer Function, proportional to apparent inductance = secant of B-H)
- **L_diff = dB1/dI** (proportional to differential inductance = local slope of B-H)

In a linear (unsaturated) magnet, L_app = L_diff = const. As iron saturates, L_diff drops faster than L_app because it tracks the local slope while L_app tracks the secant. The ratio L_diff/L_app < 1 quantifies the degree of saturation. Computed via `np.gradient(B1, I)` per branch (ascending/descending separately to preserve hysteresis information).

### cel/fed Safety Diagnostic
This notebook includes a `diagnose_cel_fed()` check that verifies the centre-location and feeddown corrections (cel/fed) are reliable. The diagnostic compares pipeline results with and without cel/fed, flags turns with |zR| > 1% of R_ref, and provides a SAFE/MIXED/UNSAFE recommendation. See `correction_options_reference.md` for background.
